apiVersion: v1
kind: Service
metadata:
  name: {{ include "suwayomi.fullname" . }}
  labels:
    {{- include "suwayomi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "suwayomi.selectorLabels" . | nindent 4 }}
