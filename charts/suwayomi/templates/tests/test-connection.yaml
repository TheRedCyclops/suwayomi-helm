apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "suwayomi.fullname" . }}-test-connection"
  labels:
    {{- include "suwayomi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "suwayomi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
